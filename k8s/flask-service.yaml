# Flask Service
apiVersion: v1
kind: Service
metadata:
  name: flask-svc
  namespace: pc-k8s
spec:
  type: NodePort
  selector:
    app: flask
  ports:
    - protocol: TCP
      port: 5001
      nodePort: 30001
